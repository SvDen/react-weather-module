var React = require('react');
var ReactDOM = require('react-dom');
var MainComponent = require('./components/MainComponent.jsx');

ReactDOM.render(<MainComponent />, document.getElementById('weather-module'));